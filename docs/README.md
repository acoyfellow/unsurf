# unsurf docs

Starlight site for `unsurf.coey.dev`.

## Local commands

```bash
bun install
bun dev
bun build
bun preview
```

The content source lives under `src/content/docs/`. The current narrative centers on:

- authenticated local browser runtime
- recording / observation / loops
- hosted MCP and local browser MCP
- reusable API seams through scout / worker / heal
